---
phase: 01-implementacao
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03
  - STRUCT-04
  - STRUCT-05

must_haves:
  truths:
    - "index.html abre no browser e exibe as 9 seções na ordem correta"
    - "Todo o copy das 9 seções está exatamente conforme COPY.md, sem paráfrases"
    - "HTML5 semântico: section, header, main, footer com ids corretos para âncoras"
    - "Meta tags preenchidas: title, description e og:tags presentes"
    - "Todos os caminhos de assets são relativos (./css/, ./js/, ./img/)"
  artifacts:
    - path: "index.html"
      provides: "Estrutura HTML completa das 9 seções"
      min_lines: 150
      contains: "<!DOCTYPE html>"
    - path: "css/style.css"
      provides: "Arquivo CSS criado (mesmo que vazio — para o link funcionar)"
    - path: "js/main.js"
      provides: "Arquivo JS criado (mesmo que vazio — para o script funcionar)"
    - path: "img/"
      provides: "Pasta de imagens criada"
  key_links:
    - from: "index.html"
      to: "./css/style.css"
      via: "link rel=stylesheet href"
      pattern: "href=\"./css/style.css\""
    - from: "index.html"
      to: "./js/main.js"
      via: "script src"
      pattern: "src=\"./js/main.js\""
---

<objective>
Criar a estrutura HTML completa da landing page com as 9 seções semânticas e o copy exato aprovado do COPY.md.

Purpose: Base sobre a qual todo o CSS e JS será construído. Sem esta estrutura, nada mais pode avançar.
Output: index.html com 9 seções, meta tags, links para CSS/JS, e pastas auxiliares criadas.
</objective>

<execution_context>
@/Users/daniloblima/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniloblima/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-implementacao/CONTEXT.md
@COPY.md
@HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar estrutura de pastas e arquivos vazios de suporte</name>
  <files>css/style.css, js/main.js, img/.gitkeep</files>
  <action>
    Criar a estrutura de pastas conforme HANDOFF.md:

    ```
    index.html        ← criado na Task 2
    css/style.css     ← criar agora (pode estar vazio)
    js/main.js        ← criar agora (pode estar vazio)
    img/              ← criar pasta, adicionar .gitkeep para git rastrear
    ```

    Todos os caminhos são relativos à raiz do projeto.
    Criar css/style.css com apenas um comentário inicial: `/* konekt.lab — Styles */`
    Criar js/main.js com apenas um comentário inicial: `/* konekt.lab — Main JS */`
    Criar img/.gitkeep como arquivo vazio.
  </action>
  <verify>
    Verificar que os arquivos existem:
    - ls css/style.css
    - ls js/main.js
    - ls img/
  </verify>
  <done>Pasta css/, pasta js/, pasta img/ e seus arquivos iniciais existem na raiz do projeto.</done>
</task>

<task type="auto">
  <name>Task 2: Criar index.html com as 9 seções e copy completo</name>
  <files>index.html</files>
  <action>
    Criar index.html completo com a seguinte estrutura e regras:

    **Cabeçalho do documento:**
    ```html
    <!DOCTYPE html>
    <html lang="pt-BR">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>konekt.lab — Estratégia Comercial para Tecnologia Aplicada</title>
      <meta name="description" content="Danilo Lima ajuda empresas de tecnologia aplicada a superar o gargalo entre boa tecnologia e tração comercial. Diagnóstico, estratégia e execução.">
      <meta property="og:title" content="konekt.lab — Estratégia Comercial para Tecnologia Aplicada">
      <meta property="og:description" content="Danilo Lima ajuda empresas de tecnologia aplicada a superar o gargalo entre boa tecnologia e tração comercial.">
      <meta property="og:type" content="website">
      <link rel="stylesheet" href="./css/style.css">
    </head>
    <body>
    ```

    **Estrutura do body:**

    `<header>` — contém apenas o logotipo/nome "konekt.lab" e a nav (sem nav por enquanto, só o nome)

    `<main>` — contém todas as sections

    `<footer>` — contém apenas copyright

    `<script src="./js/main.js"></script>` — antes de fechar body

    **As 9 seções dentro de `<main>` — usar IDs para âncoras:**

    Seção 1 — `<section id="hero" class="hero">`:
    - `<h1>`: "Sua tecnologia resolve de verdade. O mercado ainda não chegou a essa conclusão."
    - `<p class="subheadline">`: "O gargalo entre boa tecnologia e tração comercial quase nunca é técnico."
    - `<p class="body-text">`: "Não importa o estágio. Empresa nova com tecnologia de ponta tentando entrar no mercado ou empresa consolidada com novo produto que não encontrou tração. O cenário costuma ser o mesmo. A empresa fala da engenharia com entusiasmo. O cliente está pensando se aquilo resolve o problema dele ou não."
    - `<a href="#" class="cta-btn" id="cta-hero" target="_blank" rel="noopener">Agende uma conversa</a>`

    Seção 2 — `<section id="problema" class="problema">`:
    - `<h2>`: "Você já viveu alguma dessas situações?"
    - `<ol class="situacoes">` com 4 `<li>` items — copiar EXATAMENTE o texto das 4 situações do COPY.md seção 2. Não parafrasear.

    Seção 3 — `<section id="por-que" class="por-que">`:
    - `<h2>`: "Por que isso acontece"
    - `<p>`: texto completo da seção 3 do COPY.md — copiar EXATAMENTE.

    Seção 4 — `<section id="sobre" class="sobre">`:
    - `<h2>`: "Quem é Danilo"
    - `<div class="sobre-content">` com:
      - `<div class="foto-placeholder" aria-label="Foto de Danilo Lima">` — placeholder visual (div cinza com texto "Danilo Lima", a ser substituído pela foto real)
      - `<div class="bio">` com `<p>` contendo texto completo da seção 4 do COPY.md — copiar EXATAMENTE.

    Seção 5 — `<section id="servicos" class="servicos">`:
    - `<h2>`: "Serviços"
    - `<div class="cards-grid">` com 3 `<article class="card-servico">`:
      - Card 1: `<h3>Diagnóstico e Narrativa Comercial</h3>` + `<p>` com texto exato do COPY.md
      - Card 2: `<h3>Estratégia Comercial e Go-to-Market</h3>` + `<p>` com texto exato do COPY.md
      - Card 3: `<h3>Sustentação Estratégica e Cadência de Execução</h3>` + `<p>` com texto exato do COPY.md
    - IMEDIATAMENTE APÓS o fechamento de `.cards-grid`, adicionar o terceiro CTA (INTER-01):
      `<a href="#" class="cta-btn" id="cta-servicos" target="_blank" rel="noopener">Agende uma conversa</a>`

    Seção 6 — `<section id="evidencias" class="evidencias">`:
    - `<h2>`: "Evidências"
    - `<div class="cards-grid">` com 3 `<article class="card-evidencia">`:
      - Cada card tem: `<p class="situacao">` (texto antes do "Resultado:") + `<p class="resultado">` (o resultado)
      - Usar EXATAMENTE o texto do COPY.md seção 6 para cada evidência, dividindo situação e resultado.
      - Card 1 (energia): situação = "Empresa sem segmentação real de clientes B2B. Investigação de campo com 20 entrevistas em profundidade." / resultado = "Identificação de R$ 20 milhões por ano em oportunidade. Metodologia adotada em toda a América do Sul."
      - Card 2 (biogás): situação = "Setor com poucos contatos, sem estrutura prévia, prazo de três meses. Organização do zero com estratégias fora do padrão on e offline." / resultado = "100 participantes, 40 reuniões de negócio, empresas multinacionais do setor presente. Feedback do mercado: 'marcou o setor.'"
      - Card 3 (solar): situação = "Projeto de visibilidade para a empresa, mas que daria prejuízo pelos números originais." / resultado = "Construção de oportunidade comercial dentro do projeto com aditivo contratual que pagou o projeto inteiro."

    Seção 7 — `<section id="custo" class="custo">`:
    - `<h2>`: "O custo da inação"
    - `<p>`: texto completo da seção 7 do COPY.md — copiar EXATAMENTE.

    Seção 8 — `<section id="como-funciona" class="como-funciona">`:
    - `<h2>`: "Como funciona"
    - `<ol class="passos">` com 3 `<li>`:
      - `<strong>Conversa de diagnóstico</strong>` — 30 minutos para entender onde estão os maiores desafios e se faz sentido trabalharmos juntos.
      - `<strong>Proposta com escopo fechado</strong>` — Um trabalho definido, com entregável objetivo e prazo claro.
      - `<strong>Execução junto</strong>` — O trabalho acontece com você dentro, não entregue de fora.

    Seção 9 — `<section id="cta-final" class="cta-final">`:
    - `<p>`: "Se você se identificou em alguma das situações acima, o próximo passo é simples. Uma conversa de 30 minutos para entender onde estão as barreiras e como superá-las."
    - `<a href="#" class="cta-btn" id="cta-final-btn" target="_blank" rel="noopener">Agende uma conversa</a>`

    **Footer:**
    ```html
    <footer>
      <p>© 2026 konekt.lab</p>
    </footer>
    ```

    **Regras críticas:**
    - Usar href="#" nos CTAs — o JS (Plan 03) vai substituir pelo link real com UTMs
    - NÃO escrever o link do Google Calendar diretamente no HTML
    - Todos os caminhos de assets com ./ (relativo): `./css/style.css`, `./js/main.js`
    - Todos os textos das seções devem ser EXATAMENTE os do COPY.md, sem qualquer alteração
    - HTML5 semântico: usar section, article, header, main, footer corretamente
  </action>
  <verify>
    Abrir index.html no browser (duplo clique ou `open index.html`). Verificar:
    1. Página carrega sem erro de arquivo não encontrado no console
    2. As 9 seções estão visíveis (sem estilo ainda, só texto bruto)
    3. Inspecionar source: link rel=stylesheet aponta para ./css/style.css
    4. Inspecionar source: script src aponta para ./js/main.js
    5. Grep para confirmar caminhos relativos: grep -n "href=\"/" index.html deve retornar vazio (nenhum caminho absoluto)
    6. grep -c "Agende uma conversa" index.html deve retornar 3 (hero, servicos, cta-final)
  </verify>
  <done>index.html abre no browser com as 9 seções legíveis. Três CTAs presentes: #cta-hero, #cta-servicos e #cta-final-btn. Todo o copy está idêntico ao COPY.md. Nenhum caminho absoluto (/css/, /js/) presente no HTML.</done>
</task>

</tasks>

<verification>
1. `ls -la` na raiz confirma: index.html, css/style.css, js/main.js, img/
2. `open index.html` mostra 9 seções com texto legível
3. `grep -c "Agende uma conversa" index.html` retorna 3 (hero, servicos e cta-final)
4. `grep "href=\"/" index.html` retorna vazio (nenhum caminho absoluto)
5. `grep "og:title" index.html` retorna a meta og:title
6. `grep "id=\"cta-servicos\"" index.html` retorna o elemento na seção #servicos
</verification>

<success_criteria>
- index.html existe e abre no browser sem erros de console
- 9 seções com IDs: hero, problema, por-que, sobre, servicos, evidencias, custo, como-funciona, cta-final
- Três CTAs com IDs: cta-hero, cta-servicos (após .cards-grid da seção #servicos), cta-final-btn
- Copy idêntico ao COPY.md em todas as seções
- Meta tags presentes: charset, viewport, title, description, og:title, og:description, og:type
- Caminhos relativos: ./css/style.css e ./js/main.js
- Pastas criadas: css/, js/, img/
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-implementacao/01-01-SUMMARY.md` documentando:
- Arquivos criados e suas estruturas
- IDs das seções (para referência do CSS e JS)
- Classes CSS usadas (para guiar o Plan 02 e 03)
- Qualquer decisão tomada durante a implementação
</output>
