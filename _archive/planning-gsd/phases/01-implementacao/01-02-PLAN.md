---
phase: 01-implementacao
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - css/style.css
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - PERF-03

must_haves:
  truths:
    - "O arquivo css/style.css existe com variáveis CSS definidas para cores, tipografia e espaçamento"
    - "O design system tem tom visual ultra-moderno, calmo e tátil — sem excessos decorativos"
    - "Tipografia com hierarquia clara entre h1, h2, h3, p.subheadline e p.body-text"
    - "Reset CSS e box-sizing aplicados globalmente"
    - "CSS sem dependências externas — apenas CSS vanilla"
  artifacts:
    - path: "css/style.css"
      provides: "Design system completo com variáveis, reset, tipografia e layout base"
      min_lines: 120
      contains: ":root"
  key_links:
    - from: "css/style.css"
      to: "index.html"
      via: "variáveis CSS :root referenciadas pelas classes do HTML"
      pattern: "--color-|--font-|--space-"
---

<objective>
Criar o design system CSS base: variáveis, reset, tipografia e layout estrutural da página.

Purpose: Estabelecer a linguagem visual consistente (cores, tipo, espaçamento) que os componentes do Plan 03 vão herdar. Um design system bem definido aqui evita inconsistências nos planos seguintes.
Output: css/style.css com :root variables, reset, tipografia e layout de seção.
</objective>

<execution_context>
@/Users/daniloblima/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniloblima/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-implementacao/CONTEXT.md
@HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir variáveis CSS e reset global</name>
  <files>css/style.css</files>
  <action>
    Escrever o bloco :root com o design system e o reset global.

    **Paleta de cores — tom ultra-moderno, calmo, tátil:**
    ```css
    :root {
      /* Cores base */
      --color-bg: #F9F8F6;           /* Off-white quente — fundo principal */
      --color-surface: #FFFFFF;      /* Branco puro — cards e superfícies */
      --color-text: #1A1A1A;         /* Quase preto — texto principal */
      --color-text-muted: #6B6B6B;   /* Cinza médio — textos secundários */
      --color-accent: #1A1A1A;       /* Acento primário — mesmo que texto (clean) */
      --color-border: #E5E3DF;       /* Borda sutil — cards, divisores */
      --color-cta-bg: #1A1A1A;       /* Fundo do botão CTA */
      --color-cta-text: #F9F8F6;     /* Texto do botão CTA */
      --color-placeholder: #D4D0C8;  /* Cinza para placeholder de foto */

      /* Tipografia */
      --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      --font-size-xs: 0.75rem;    /* 12px */
      --font-size-sm: 0.875rem;   /* 14px */
      --font-size-base: 1rem;     /* 16px */
      --font-size-lg: 1.125rem;   /* 18px */
      --font-size-xl: 1.375rem;   /* 22px */
      --font-size-2xl: 1.75rem;   /* 28px */
      --font-size-3xl: 2.25rem;   /* 36px */
      --font-size-4xl: 3rem;      /* 48px */
      --line-height-tight: 1.2;
      --line-height-base: 1.6;
      --line-height-relaxed: 1.75;

      /* Espaçamento (base 8px) */
      --space-1: 0.5rem;    /* 8px */
      --space-2: 1rem;      /* 16px */
      --space-3: 1.5rem;    /* 24px */
      --space-4: 2rem;      /* 32px */
      --space-6: 3rem;      /* 48px */
      --space-8: 4rem;      /* 64px */
      --space-12: 6rem;     /* 96px */
      --space-16: 8rem;     /* 128px */

      /* Layout */
      --max-width: 760px;           /* Largura máxima do conteúdo — editorial, não larga */
      --max-width-wide: 1024px;     /* Para grids de cards */
      --border-radius-sm: 4px;
      --border-radius-md: 8px;
      --border-radius-lg: 16px;

      /* Transições */
      --transition-fast: 150ms ease;
      --transition-base: 250ms ease;
    }
    ```

    **Reset e base global:**
    ```css
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      font-size: 16px;
      scroll-behavior: smooth;
    }

    body {
      font-family: var(--font-sans);
      font-size: var(--font-size-base);
      line-height: var(--line-height-base);
      color: var(--color-text);
      background-color: var(--color-bg);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    img {
      max-width: 100%;
      height: auto;
      display: block;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    ol, ul {
      list-style: none;
      padding: 0;
    }
    ```

    **Importar Inter via Google Fonts no topo do CSS (sem adicionar ao HTML):**
    ```css
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap');
    ```
    Colocar esta linha como PRIMEIRA linha do arquivo, antes de :root.

    Nota: Inter via Google Fonts é uma única requisição HTTP externa mas com <1 KB de overhead. Aceitável dado PERF-03 que diz "sem dependências desnecessárias" — Inter é necessária para o tom visual pretendido.
  </action>
  <verify>
    Inspecionar css/style.css:
    - Primeira linha: @import com Inter
    - Bloco :root presente com as variáveis listadas
    - Reset global aplicado (*, html, body)
  </verify>
  <done>css/style.css contém @import Inter, :root com todas as variáveis de design, e reset global.</done>
</task>

<task type="auto">
  <name>Task 2: Estilos de tipografia, seções e layout base</name>
  <files>css/style.css</files>
  <action>
    Adicionar ao css/style.css (continuando após o reset):

    **Container central:**
    ```css
    .container {
      width: 100%;
      max-width: var(--max-width);
      margin-left: auto;
      margin-right: auto;
      padding-left: var(--space-4);
      padding-right: var(--space-4);
    }

    .container--wide {
      max-width: var(--max-width-wide);
    }
    ```

    **Header:**
    ```css
    header {
      padding: var(--space-3) var(--space-4);
      display: flex;
      align-items: center;
    }

    .logo {
      font-size: var(--font-size-base);
      font-weight: 600;
      letter-spacing: -0.02em;
      color: var(--color-text);
    }
    ```

    **Seções — padding vertical padrão:**
    ```css
    section {
      padding-top: var(--space-12);
      padding-bottom: var(--space-12);
    }
    ```

    **Tipografia de headings:**
    ```css
    h1 {
      font-size: var(--font-size-3xl);
      font-weight: 600;
      line-height: var(--line-height-tight);
      letter-spacing: -0.03em;
      color: var(--color-text);
      margin-bottom: var(--space-3);
    }

    h2 {
      font-size: var(--font-size-2xl);
      font-weight: 600;
      line-height: var(--line-height-tight);
      letter-spacing: -0.02em;
      color: var(--color-text);
      margin-bottom: var(--space-4);
    }

    h3 {
      font-size: var(--font-size-lg);
      font-weight: 600;
      line-height: var(--line-height-tight);
      color: var(--color-text);
      margin-bottom: var(--space-2);
    }

    p {
      margin-bottom: var(--space-3);
    }

    p:last-child {
      margin-bottom: 0;
    }
    ```

    **Hero — tipografia específica:**
    ```css
    .hero {
      padding-top: var(--space-16);
      padding-bottom: var(--space-16);
      background-color: var(--color-bg);
    }

    .hero .container {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }

    .subheadline {
      font-size: var(--font-size-xl);
      font-weight: 400;
      color: var(--color-text-muted);
      line-height: var(--line-height-base);
      margin-bottom: 0;
    }

    .body-text {
      font-size: var(--font-size-lg);
      line-height: var(--line-height-relaxed);
      color: var(--color-text);
      max-width: 640px;
    }
    ```

    **CTA Button:**
    ```css
    .cta-btn {
      display: inline-block;
      background-color: var(--color-cta-bg);
      color: var(--color-cta-text);
      font-size: var(--font-size-base);
      font-weight: 500;
      padding: var(--space-2) var(--space-4);
      border-radius: var(--border-radius-md);
      transition: opacity var(--transition-fast), transform var(--transition-fast);
      min-height: 44px;            /* RESP-04: área de toque mínima mobile */
      line-height: 44px;
      align-self: flex-start;
    }

    .cta-btn:hover {
      opacity: 0.85;
      transform: translateY(-1px);
    }

    .cta-btn:active {
      transform: translateY(0);
    }
    ```

    **Seção problema — lista numerada:**
    ```css
    .problema .container {
      /* herda estilos de seção */
    }

    .situacoes {
      list-style: decimal;
      padding-left: var(--space-4);
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
    }

    .situacoes li {
      font-size: var(--font-size-lg);
      line-height: var(--line-height-relaxed);
      color: var(--color-text);
      padding-left: var(--space-2);
    }
    ```

    **Seção por-que — texto corrido:**
    ```css
    .por-que {
      background-color: var(--color-surface);
    }

    .por-que p {
      font-size: var(--font-size-lg);
      line-height: var(--line-height-relaxed);
    }
    ```

    **Seção sobre — layout com foto:**
    ```css
    .sobre {
      /* fundo padrão */
    }

    .sobre-content {
      display: flex;
      flex-direction: column;
      gap: var(--space-6);
    }

    .foto-placeholder {
      width: 180px;
      height: 180px;
      background-color: var(--color-placeholder);
      border-radius: var(--border-radius-md);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: var(--font-size-sm);
      color: var(--color-text-muted);
      font-weight: 500;
      flex-shrink: 0;
    }

    .bio p {
      font-size: var(--font-size-base);
      line-height: var(--line-height-relaxed);
    }
    ```

    **Footer:**
    ```css
    footer {
      padding: var(--space-6) var(--space-4);
      border-top: 1px solid var(--color-border);
      text-align: center;
    }

    footer p {
      font-size: var(--font-size-sm);
      color: var(--color-text-muted);
      margin-bottom: 0;
    }
    ```

    **Seção custo da inação:**
    ```css
    .custo {
      background-color: var(--color-surface);
    }

    .custo p {
      font-size: var(--font-size-lg);
      line-height: var(--line-height-relaxed);
    }
    ```

    **Seção como funciona — lista numerada de passos:**
    ```css
    .passos {
      list-style: decimal;
      padding-left: var(--space-4);
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
      counter-reset: none;
    }

    .passos li {
      font-size: var(--font-size-lg);
      line-height: var(--line-height-relaxed);
      padding-left: var(--space-2);
    }

    .passos li strong {
      font-weight: 600;
      display: block;
      margin-bottom: var(--space-1);
    }
    ```

    **Seção CTA final:**
    ```css
    .cta-final {
      background-color: var(--color-bg);
      text-align: center;
    }

    .cta-final .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-4);
    }

    .cta-final p {
      font-size: var(--font-size-lg);
      line-height: var(--line-height-relaxed);
      max-width: 600px;
    }
    ```

    **IMPORTANTE:** Cada section no HTML precisa envolver seu conteúdo em `<div class="container">`. Verificar se o HTML do Plan 01 tem isso. Se não tiver, anotar no SUMMARY para o executor do Plan 03 corrigir (ou corrigir agora adicionando as divs container no index.html).

    Nota: O CSS deste plan é escrito sem ver o HTML final do Plan 01 (execução paralela). As classes usadas (.hero, .problema, .sobre-content, etc.) correspondem às classes definidas no Plan 01. Qualquer discrepância de classe deve ser resolvida pelo executor do Plan 03.
  </action>
  <verify>
    Verificar que css/style.css tem pelo menos 150 linhas:
    `wc -l css/style.css`

    Abrir index.html no browser após este plan executar. A página deve mostrar tipografia limpa com hierarquia clara, sem o caos de CSS não aplicado.
  </verify>
  <done>css/style.css contém design system completo: @import Inter, :root com variáveis, reset, container, tipografia de headings, estilos de hero/seções/footer. Página visualmente coerente ao abrir no browser.</done>
</task>

</tasks>

<verification>
1. `wc -l css/style.css` retorna 150+ linhas
2. `grep -c "var(--" css/style.css` retorna 20+ (variáveis sendo usadas)
3. Abrir index.html confirma: tipografia Inter aplicada, hierarquia de headings visível
4. Nenhum cdn externo além de Google Fonts (@import Inter)
</verification>

<success_criteria>
- css/style.css tem :root com variáveis para cores (8+), tipografia (10+), espaçamento (8+), layout
- Reset global aplicado
- Tom visual ultra-moderno e calmo: off-white #F9F8F6, texto quase preto, sem cores vibrantes
- Hierarquia tipográfica clara: h1 > h2 > h3 > p.subheadline > p
- CTA button com min-height 44px (mobile touch target)
- Sem frameworks CSS externos — apenas Inter via Google Fonts
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-implementacao/01-02-SUMMARY.md` documentando:
- Paleta de cores definida (hex values)
- Escala tipográfica definida
- Variáveis CSS criadas (lista completa)
- Qualquer classe CSS que o Plan 03 precisa saber
</output>
